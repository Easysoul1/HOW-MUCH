# ── Required ──
SECRET_KEY=your-long-random-secret-key-here
ALLOWED_HOSTS=your-app.railway.app,localhost

# ── Database (mount a volume at /data on Railway/Render) ──
DATABASE_PATH=/data/db.sqlite3

# ── Cloudinary (media uploads) ──
CLOUDINARY_CLOUD_NAME=dortiuxs0
CLOUDINARY_API_KEY=983643598664176
CLOUDINARY_API_SECRET=YGpyckFipVy1h2o0t2-Hx6SAE5Y

# ── Optional ──
DEBUG=False
# NEXT_PUBLIC_API_URL defaults to /api (same-domain via nginx) — only set this
# if you split frontend/backend onto separate domains in the future.
